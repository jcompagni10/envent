import  {StyleSheet} from 'react-native';

export default StyleSheet.create({
  ticketWrapper: {
    top: 50,
    flex: 1,
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  gradient:{
    height: '100%',
    width: '100%'
  }

});
